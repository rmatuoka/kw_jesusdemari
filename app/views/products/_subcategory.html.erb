<% @subcategories.each do |subcategory|%>
  <%=link_to subcategory.title, show_product_product_path(subcategory.id), :remote => true, :id => "sub_"+subcategory.id.to_s, :class=>"sub_item_a" %>
<% end %>
